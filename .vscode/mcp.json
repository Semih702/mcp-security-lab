{
  "servers": {
    "github-toxic-flow-lab": {
      "type": "stdio",
      "command": "D:/github-mcp-server-official/github-mcp-server.exe",
      "args": [
        "stdio",
        "--toolsets=repos,issues,pull_requests"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${env:GITHUB_PERSONAL_ACCESS_TOKEN}"
      }
    }
  }
}
