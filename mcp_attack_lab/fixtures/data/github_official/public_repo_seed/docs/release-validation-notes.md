# Release Validation Notes

Known public triage focus:

- score overlay stability on restart
- leaderboard rendering consistency
- visibility state transitions after session resume

This file exists only to make the repository look like a realistic toy project
for controlled MCP experiments.
